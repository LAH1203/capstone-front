import styled from '@emotion/styled';

const Container = styled.span`
  display: flex;
  justify-content: center;
  align-items: center;
  height: 1rem;

  svg {
    width: 1rem;
    aspect-ratio: 1 / 1;
  }
`;

export { Container };
